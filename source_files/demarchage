:name: Démarchage

:description:
  Liste pour campagne d'adhésion de tiers n'ayant jamais adhéré.<br>
  Le type de tiersest à renseigner.<br>
  Le format de sortie est tiers_id, infos_complémentaires,
  ce qui permet de voir que certains ont été aboonés ou sont actuellement abonnés.

:query:
  SELECT TIERS_DESC, vue_tiers_infos_complementaires
  FROM tiers
  LEFT JOIN vue_tiers_infos
    ON tiers_id = vue_tiers_infos_tiers
  LEFT JOIN type_tiers
    ON tiers_type  =type_tiers_id
  WHERE tiers.tiers_etat = 1
    AND type_tiers_nom = PARAM
    AND vue_tiers_infos_complementaires NOT REGEXP 'Mb'

:parameter_list:
- Individu
- Institution

